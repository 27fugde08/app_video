<UserControl x:Class="CreatorOS.Desktop.Wpf.Views.TimelineEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:CreatorOS.Desktop.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="#0F172A">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <!-- Top Controls Toolbar -->
            <RowDefinition Height="Auto"/>
            <!-- Video Canvas Preview -->
            <RowDefinition Height="380"/>
            <!-- Interactive Multi-Track Timeline -->
            <RowDefinition Height="*"/>
            <!-- Bottom Export Status -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Controls Toolbar -->
        <Border Grid.Row="0" Background="#1E293B" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="#334155" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Studio Content Editor" FontSize="18" FontWeight="SemiBold" Foreground="#F8FAFC" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="{Binding IsPlaying, ConverterParameter='⏸ Tạm Dừng:▶ Phát', Converter={x:Null}}" 
                            Background="#0284C7" Foreground="#FFFFFF" BorderThickness="0" Padding="12,6" Margin="0,0,8,0" Command="{Binding TogglePlayPauseCommand}"/>
                    <Button Content="✂️ Cắt Phân Đoạn (C)" Background="#334155" Foreground="#F8FAFC" BorderThickness="0" Padding="12,6" Margin="0,0,8,0" Command="{Binding SplitClipAtPlayheadCommand}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="🚀 Xuất Video NVENC (MP4)" Background="#10B981" Foreground="#FFFFFF" FontWeight="SemiBold" BorderThickness="0" Padding="14,6" Command="{Binding ExportVideoCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Video Canvas Direct3D 11 Preview -->
        <Border Grid.Row="1" Background="#020617" CornerRadius="8" Margin="0,0,0,8" BorderBrush="#334155" BorderThickness="1" ClipToBounds="True">
            <views:D3DVideoCanvas Source="{Binding ActivePreviewSource}" Position="{Binding CurrentPositionSeconds}"/>
        </Border>

        <!-- Interactive Vector Multi-Track Timeline -->
        <Border Grid.Row="2" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" ClipToBounds="True">
            <views:TimelineControl Project="{Binding Project}" CurrentPositionSeconds="{Binding CurrentPositionSeconds, Mode=TwoWay}" PixelsPerSecond="45"/>
        </Border>

        <!-- Bottom Export Progress / Status -->
        <Border Grid.Row="3" Background="#1E293B" CornerRadius="6" Padding="8,6" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding ExportStatusMessage}" Foreground="#94A3B8" FontSize="12" VerticalAlignment="Center"/>
                <ProgressBar Grid.Column="1" Value="{Binding ExportProgress}" Maximum="100" Width="180" Height="8" Background="#334155" Foreground="#10B981" BorderThickness="0"
                             Visibility="{Binding IsExporting, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
